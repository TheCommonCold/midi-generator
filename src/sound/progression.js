export class Progression{
    constructor({notes, rythm, genome, notes2, score}){
        this.notes = notes
        this.rythm = rythm
        this.genome = genome
        this.notes2 = notes2
        this.score = score
    }

    // transpose(notes, value){
    //     return notes.map(notes => notes.map(note => note + value))
    // }
}
